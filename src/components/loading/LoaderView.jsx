// LoaderView.js
import React from 'react';
import Loading from './Loading'; // Asegúrate de que la ruta sea correcta

const LoaderView = () => {
  return (
    <div>
      
    <h1>Visualización del Componente Loader</h1>
    
      <Loading />
      {/* Other JSX */}
    </div>
  );
};

export default LoaderView;
